# Chitra — Python Dependencies
# Install: pip install -r requirements.txt

# Environment configuration
python-dotenv==1.0.1

# Local LLM client (Ollama HTTP API)
httpx==0.27.0

# Speech-to-text (local Whisper)
openai-whisper==20231117

# Voice activity detection
silero-vad==5.1

# Audio I/O
sounddevice==0.4.7
numpy==1.26.4

# Text-to-speech (Piper — invoked as subprocess, no pip package)
# Piper binary downloaded by scripts/setup_piper.py

# Conversational display
rich==13.7.1

# Testing
pytest==8.1.1
pytest-asyncio==0.23.6
