# Chitra — CI-only Dependencies (lightweight, no ML/audio)
# Full deps including whisper/torch: pip install -r requirements.txt

# Environment configuration
python-dotenv==1.2.1

# Local LLM client (Ollama HTTP API)
httpx==0.28.1

# Audio I/O (lightweight — no torch)
sounddevice==0.5.5
numpy==2.4.2

# Conversational display
rich==14.3.3

# Testing
pytest==9.0.2
pytest-asyncio==1.3.0
